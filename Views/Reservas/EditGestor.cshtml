@model MyAirbnb.Models.Reserva

@{
    ViewData["Title"] = "Edit";
}

<h1>Editar</h1>

<h4>Reserva</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="EditGestor">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <input type="hidden" asp-for="DateStart" />
            <input type="hidden" asp-for="DateEnd" />
            <input type="hidden" asp-for="ImovelId" />
            <div class="form-group">
                <label asp-for="RatingCliente" class="control-label"></label>
                <input asp-for="RatingCliente" class="form-control" />
                <span asp-validation-for="RatingCliente" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CommentCliente" class="control-label"></label>
                <input asp-for="CommentCliente" class="form-control" />
                <span asp-validation-for="CommentCliente" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Guardar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Voltar à lista</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
